<UserControl x:Class="MailSender.Views.RecipientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MailSender.Views"
             xmlns:viewModels="clr-namespace:MailSender.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Получатели" HorizontalAlignment="Center" FontWeight="Bold"/>
        <DataGrid DockPanel.Dock="Left" ItemsSource="{Binding (viewModels:MainWindowViewModel.Recipients)}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="None"
                  VerticalScrollBarVisibility="Visible"
                  AlternatingRowBackground="Gainsboro">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button DockPanel.Dock="Bottom" HorizontalAlignment="Center" Content="Обновить"/>
    </DockPanel>
</UserControl>
